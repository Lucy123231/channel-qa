# 渠道管理智能客服 · 交接与维护说明

> 最后更新：2026-08-19 ｜ 版本：v5 · 纯本地版 ｜ 线上地址：https://lucy123231.github.io/channel-qa/

## 一、这是什么

- 一个**纯本地、零服务器、零API调用**的单页智能客服（一个 HTML 文件 = 整个系统）。
- 知识来源：《渠道管理指南》（6域21场景：一批商/二批商/合同电子签/库存/仓库地址/一码通办与奖励会谈）。
- 全部问答逻辑是**确定性的规则引擎**（关键词+索引+菜单+强制路由），不接大模型，回答稳定可测。
- 判断用户看到的是不是最新版：页面左上角有构建时间戳「**v5 · 构建 YYYY-MM-DD HH:MM**」。

## 二、文件结构（这些必须保留）

```
index.html               构建产物（线上运行的页面，每次构建自动生成，需提交）
.nojekyll                GitHub Pages 必需，勿删
kb/
  kb.json                知识库数据（日常改内容改这里）
  images/                147张业务截图（文件名即图编号，被 kb.json 引用）
  README.md              知识库字段说明（简版）
src/
  template.html          客服引擎全部逻辑（检索/规则/菜单/输出，约1000行）
  build.py               构建脚本（压缩图片→base64、注入知识库、拼音表、时间戳）
  verify.js              回归测试（225项，改完必须跑全绿）
  requirements.txt       Python 依赖（Pillow、pypinyin）
交接说明.md              本文档（唯一入口，交给接手人先读这个）
渠道管理AI问答业务规则与解决方案-给IT.md   业务背景文档（历史规则汇总，可选阅读）
```

本地还有两个**不入库**的目录，可删可留：
- `.claude/`：Claude Code 的辅助配置（含规则文档 channel-management.md，供 AI 辅助维护时参考）。
- `.vscode/`：编辑器设置。

## 三、日常修改三步走

> 🎯 **零门槛模式（推荐给新手）**：只改内容的场景，**完全不需要在本地装 Python**——直接登录 github.com，打开 `kb/kb.json` 点铅笔图标改文字 → 提交（Commit）。GitHub Actions 会自动在云端构建网页、跑225项测试、发布上线（约2-3分钟）。测试不过会自动中止、不发布，旧版本保持在线。
> 本地完整流程（改逻辑/新增截图时用）见下文。

### 第1步：改内容（最常见）——只动 kb/kb.json

每个分类下是一组章节 `s[]`，字段含义：

| 字段 | 说明 | 示例 |
|---|---|---|
| `title` | 条目标题（`\n` 换行） | `"1、常规开户（渠道改造新拓…）"` |
| `tx` | 正文（`\n` 换行） | `"TPM系统→实控人档案管理录入…"` |
| `img` / `imgs` | 截图文件名（单张/多张） | `"2.png"` / `["44.png","45.png"]` |
| `w` | 警示提示（红底黄底展示） | `"若基本信息未同步…"` |
| `kw` | 分类级搜索关键词 | `["开户","新开","怎么开户"]` |

**关键词经验**（来自实战）：全称简称都要收录（如「实控人导入」+「实际控制人导入」）；加口语、错别字、问法变体；长词优先命中，所以越具体的词越能直达。

### 第2步：加/换截图

1. 新图放进 `kb/images/`（文件名即图编号，如 `150.png`）。
2. 在 kb.json 对应条目加 `"img": "150.png"` 或 `"imgs": [...]`。
3. 删除旧图时，同步检查没有其他条目再引用它。

### 第3步：构建 + 测试 + 提交

```bash
# 首次准备（一次）
pip install Pillow pypinyin

# 构建（在 src 目录下）
cd src
python build.py            # 输出 ../index.html；会打印 147/147 图片是否齐全

# 回归测试（必须全绿）
node verify.js             # 225 项全 PASS 才能提交

# 提交发布
git add .. && git commit -m "说明文字" && git push origin main
```

- push 后 **GitHub Actions 自动在云端完成构建+测试+提交产物**（`.github/workflows/build.yml`），随后 GitHub Pages 自动部署，全程 2-5 分钟生效；页面 HTML 有约 10 分钟 CDN 缓存（看左上角时间戳判断新旧）。
- 云端测试 225 项有失败时会中止发布，线上保持旧版——这是安全兜底，看到失败去仓库 Actions 页看是哪条测试红了。
- 公司网络不稳时 push 会失败，多重试几次即可。

## 四、规则引擎地图（src/template.html 关键位置）

| 区域 | 作用 | 怎么调 |
|---|---|---|
| `RED` | 合规红线12条（骑缝章/预包装/按手印/张雅寒/海南甲方/三批P/临期酒/渠道调整/隐藏库/拼车/特约转/推送函法务），命中自动红字高亮 | 加新红线：往数组加 `{k:[触发词],m:'提示语'}` |
| `SYN` | 同义词替换（口语→规范词） | 加 `'口语':'规范词'` |
| `IDX` | **各板块细分条目索引** `{n编号, na名称, kw关键词, c分类, s章节}`——细分直达的核心 | **新增细分条目就在这里加**，kw 要全称简称都收录；s 指 kb.json 该分类下的章节序号 |
| `ATT` | 附件1-10 索引（「附件N」直达） | 改附件要求对应 kb.json 章节 |
| `CM` / `WM` / `IM` | 合同1-26分组菜单 / 仓库19场景菜单 / 库存4组菜单 | 调分组、调编号、改名称改这里 |
| `BIZ` / `ERR` | 内部索引ID（`bizXX`/`errXX` 文本直达；纯数字不触发） | 文档新增ID时同步加 |
| 强制路由区 | 一码通办/奖励会谈/开户附件清单/26年5子项(HT26KW)/25年地区专项(海南广州电子签) | 新板块要「强制优先」时仿照加一段 |
| `HT26T`/`cut26`/`NAV26`/`hasNav`/`cth` | 26年5子项硬截断 + 导航文字黑名单自检 + 分组头剥离 | 一般不用动 |
| `_gp` | 分组菜单组内编号（从1开始）状态 | 新分组菜单要组内编号时复用 |
| `cnt()` | 结尾联系人（吴宜谦/唐鉴均/李艺航/宋子豪 按业务分流） | 改联系人改这里 |

## 五、当前已固化的交互规则（接手人必读，改坏了要能恢复）

1. **细分直达**：问具体条目（名字/编号/关键词）→ 只回那一条的正文+截图，绝不倒出整板块列表。
2. **问全部才给菜单**：只报板块/分组名（如「合同」「26年合同订立」「实际控制人」）→ 出编号菜单供选择；分组菜单**组内从1编号**。
3. **完整输出仅三种情况**：输 `0`；明确说「全部流程/完整流程/全部步骤」；点名多个编号（如「4、5、6全部给我」）。
4. **合同板块导航零容忍**：具体回答里禁止出现「合同板块全部业务如下」「25年/26年经销商合同订立」等导航文字；含则整条放弃，回「暂时无法获取该条目，请换一种说法重新提问」。
5. **分组隔离**：25年仅1-3、26年仅4-9、进度10-11、答疑12-20、推送函21-26，跨组严禁混内容。
6. **年份隔离**：带26年/新合同的问句只走4-9；带25年/老合同只走1-3。
7. **边界兜底**：不在指南范围内的问句统一回固定模板（可搜「暂不在渠道管理指南范围内」修改）。
8. 以上全部有测试锁死（verify.js），**任何改动导致测试变红，先查是不是破坏了这些规则**。

## 六、全文件功能对照表（想改什么 → 找哪个文件哪一段）

> 风险分级：🟢低（随便改）｜🟡中（有225项测试兜底，测不过不发布）｜🔴高（核心流程，必须本地跑测试全绿再提交）

### 1. `kb/kb.json` —— 知识库内容 🟢低

| 改什么 | 找哪里 | 说明 |
|---|---|---|
| 条目文字/标题 | 各分类下 `s[].title` / `s[].tx` | `\n` 表示换行 |
| 条目截图 | `s[].img`（单张）/ `s[].imgs`（多张） | 文件名与 images 文件夹里一致 |
| 警示提醒 | `s[].w` | 红字展示 |
| 搜索关键词 | 分类顶部 `kw` 数组 | **全称简称都要收录**；词越长优先级越高 |
| 板块标题/简介 | 分类 `t` / `intro` | |
| 版本号 | 顶部 `_meta.ver` | 每次政策大改更新 |

### 2. `kb/images/` —— 截图文件夹 🟢低

- 加图：网页上 Add file → Upload files 拖进去；文件名即图编号（如 `150.png`）。
- 删图：先确认 kb.json 没有条目再引用它（构建时打印 147/147 检查）。

### 3. `src/template.html` —— 外观 + 引擎（分两半）

**上半部：页面外观 🟢低（约1-54行 `<style>` 段）**

| 改什么 | 搜什么 | 说明 |
|---|---|---|
| 字体大小 | `font-size` | 数字改大改小即可 |
| 主题色（雪花红） | `#c41e2a` | |
| 气泡/间距/边框 | `padding` / `margin` / `border` | |
| 页面标题 | `<title>` | |

**下半部：问答引擎（约86行起，全部有中文注释）**

| 改什么 | 搜什么 | 约第几行 | 风险 |
|---|---|---|---|
| 板块清单 | `TPL` | 87 | 🟡 |
| **细分条目索引（新增可直达条目/加关键词）** | `IDX` | 96 | 🟡 |
| 附件1-10（「附件N」直达） | `ATT` | 204 | 🟡 |
| 同义词（口语→规范词） | `SYN` | 216 | 🟢 |
| 合同1-26分组菜单 | `CM` | 274 | 🟡 |
| 仓库19场景菜单 | `WM` | 230 | 🟡 |
| 库存4组菜单 | `IM` | 252 | 🟡 |
| 合规红线（骑缝章等12条） | `RED` | 348 | 🟢 |
| 内部索引ID（biz/err直达） | `BIZ` / `ERR` | 323 | 🟢 |
| 26年合同5子项截断/路由 | `HT26T` / `26年合同5子项强制路由` | 218 / 710 | 🔴 |
| 合同导航文字黑名单自检 | `NAV26` / `hasNav` | 79 | 🔴 |
| 分组头剥离 | `cth` | 77 | 🔴 |
| 年份隔离（25年1-3/26年4-9） | `合同年份隔离` | 704 | 🔴 |
| 分组菜单/组内编号 | `合同板块分组菜单` / `_gp` | 723 | 🔴 |
| 多编号点名（4、5、6全部给我） | `合同板块多编号点名` | 560 | 🟡 |
| 编号直达（锁定板块输数字） | `合同板块编号直达` | 511 | 🟡 |
| 底部联系人（吴宜谦/唐鉴均/李艺航/宋子豪） | `cnt` | 360 | 🟢 |
| 兜底话术（问不到时的固定回复） | 搜 `暂不在渠道管理指南范围内` | 末尾 | 🟢 |
| 主流程与各种门（send函数） | `function send` | 459 | 🔴 |

### 4. `src/build.py` —— 构建参数 🟢低

| 改什么 | 搜什么 | 说明 |
|---|---|---|
| 页面体积/加载速度/图片清晰度 | `800`（最大宽度）和 `45`（压缩质量） | 都调小→更快更糊；都调大→更清晰更慢 |
| 构建时间戳格式 | `__BUILD_STAMP__` | 一般不动 |

### 5. `src/verify.js` —— 225项回归测试 🟡中

- 新增/修改功能时，在文件末尾照抄已有格式加对应测试用例（`askA` 只看回复、`check` 断言）。
- 永远不要为了"让测试通过"去删掉旧测试。

### 6. `.github/workflows/build.yml` —— 云端自动构建 🟡中

- 触发条件：`kb/**`、`src/template.html`、`src/build.py` 有改动即自动构建+测试+提交。
- 一般不动；要加触发范围改 `paths`。

### 7. 永远不要手改的文件 🔴

- `index.html`：自动生成的成品（5.6MB含147张图），改了也会被下一次构建覆盖。
- `.nojekyll`：GitHub Pages 必需。

### 8. 文档类 🟢

- `交接说明.md`（本文档）、`kb/README.md`、`渠道管理AI问答业务规则与解决方案-给IT.md`：只改文档不影响网页。

## 七、接手人如何把改动同步到线上（GitHub）

**核心：本地改动不会自动同步，必须 `git push origin main`。** 线上网页由 GitHub Pages 监听 main 分支自动部署（push 后 1-3 分钟生效，另有约 10 分钟 CDN 缓存，看页面左上角时间戳判断新旧）。

两种拿到项目的方式：

- **方式A（推荐）：从 GitHub 克隆** —— `git clone https://github.com/Lucy123231/channel-qa`，自带 git 记录；改完构建+测试全绿后 `git add . && git commit -m "说明" && git push origin main` 即可同步。
- **方式B：只拿到压缩包（无 .git）** —— 改动推不上去，需要先按方式A克隆一份，再把自己改过的 kb.json / template.html / 新截图 / 新构建的 index.html 复制进克隆文件夹，跑 `node verify.js` 全绿后 push（或直接在克隆出来的副本上重改）。

**权限**：仓库在账号 Lucy123231 名下，新账号必须被添加为协作者才能 push：
仓库页 → Settings → Collaborators → Add people → 输入对方 GitHub 账号 → 对方接受邀请邮件。

## 八、交接清单（checklist）

- [ ] 仓库地址已给：https://github.com/Lucy123231/channel-qa（`git clone` 即得全部文件）
- [ ] 本地装好：Python 3 + `pip install Pillow pypinyin` + Node.js
- [ ] 能跑通：`cd src && python build.py` → `node verify.js` 全绿
- [ ] 知道改内容动 kb.json、改逻辑动 template.html、**不要手改 index.html**
- [ ] 知道发布 = git push，验证 = 看网页左上角时间戳
- [ ] 业务背景文档（给IT.md）与 kb/README.md 已随仓库走

> 提醒：原 `.env` 中的 API 密钥已删除（纯本地版不需要任何密钥）；接手人如需接入大模型，需要重新评估架构（当前引擎是纯规则，无模型接口）。
